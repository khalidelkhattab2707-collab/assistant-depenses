## 1. Bootstrap Pagination Setup

- [x] 1.1 Add `useBootstrapFive()` in `AppServiceProvider::boot()` to enable Bootstrap 5 pagination styling
- [x] 1.2 Verify the pagination partial views are properly generated by Laravel

## 2. Fix Bugs in Existing `recus/index.blade.php`

- [x] 2.1 Fix `$recu->texte_brut` → `$recu->text_brut` (column name mismatch)
- [x] 2.2 Fix `match($recu->statut)` → `match($recu->status)` (attribute name mismatch)
- [x] 2.3 Add `{{ $recus->links() }}` for pagination links at the bottom of the table
- [x] 2.4 Verify the view renders correctly with sample data

## 3. Implement `depenses/index.blade.php`

- [x] 3.1 Create the full view extending `layouts.app` with Bootstrap table listing expenses
- [x] 3.2 Add columns: Libellé, Quantité, Prix unitaire, Catégorie (with enum label), Reçu (linked)
- [x] 3.3 Add category filter as a Bootstrap nav/pills component above the table
- [x] 3.4 Add `{{ $depenses->links() }}` for pagination
- [x] 3.5 Add empty state message "Aucune dépense pour l'instant."
- [x] 3.6 Verify the view with the `$depenses` and `$categories` data from `DepenseController::index()`

## 4. Navigation & Routes

- [x] 4.1 Verify `layouts/app.blade.php` navigation links to `recus.index` and `depenses.index` are correct
- [x] 4.2 Verify all routes match the controller method signatures

## 5. Verification

- [x] 5.1 Run `php artisan test` to ensure all tests pass
- [ ] 5.2 Check for N+1 queries using Laravel Debugbar on all list pages
- [ ] 5.3 Verify Bootstrap pagination renders correctly on both list pages
